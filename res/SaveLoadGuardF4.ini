; SaveLoadGuardF4 0.1.0
; Prevention is enabled by default. The DLL still fails closed when its
; loaded-image callsite contract is missing, ambiguous, or in conflict.
Enabled=1
Mode=Prevent
DetailedIncidents=0
AllowUnqualifiedSuppression=1
